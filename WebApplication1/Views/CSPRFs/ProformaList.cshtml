@using WebApplication1.Utility;

@model List<WebApplication1.CSPRF>




@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>ProformaList</title>

    @Styles.Render("~/Content/css")

    <style>
        media print {
            page-break-after: always;
        }
    </style>
</head>
<body>



    <div>
        @foreach (CSPRF rec in @Model)
        {
            @Html.Action("ProformaM", new { id = MyHtmlHelpers.ConvertIdToByteStr(rec.PRFNO) })

            @*<div style="clear:both!important;"></div>*@
            <div style="page-break-after:always"></div>
            @*<div style="clear:both!important;"> </div>*@
        }
    </div>
</body>
</html>
